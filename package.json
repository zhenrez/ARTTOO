{
  "name": "artoo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/document.js && node --check src/asset-store.js && node --check src/draft-store.js && node --check src/editor-adapter.js && node --test"
  },
  "engines": { "node": ">=20" }
}
