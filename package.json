{
  "name": "artoo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/document.js && node --test"
  },
  "engines": { "node": ">=20" }
}
